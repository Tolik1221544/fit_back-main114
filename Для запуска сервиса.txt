# Запуск
sudo systemctl start fitnessapi

# Остановка
sudo systemctl stop fitnessapi

# Перезапуск
sudo systemctl restart fitnessapi

# Статус (показывает запущен ли сервис)
sudo systemctl status fitnessapi

# Логи (последние 50 строк)
sudo journalctl -u fitnessapi -n 50

# Логи в реальном времени
sudo journalctl -u fitnessapi -f

# Отключить автозапуск
sudo systemctl disable fitnessapi
sudo systemctl stop fitnessapi
2. Исправьте проблемы с базой данных
bashcd /var/www/fitnessapi/fit_back-main

# Создайте бэкап
cp fitness.db fitness_backup_$(date +%Y%m%d_%H%M%S).db

# Пересоздайте базу данных
rm fitness.db
rm -rf Migrations/
dotnet ef migrations add InitialCreate
dotnet ef database update
3. Проверьте компиляцию
bashdotnet build
4. Запустите API
bashsudo systemctl start fitnessapi
sudo systemctl status fitnessapi
5. Проверьте логи
bashsudo journalctl -u fitnessapi -f