cd /var/www/fitnessapi/fit_back-main

# Установка EF Tools (если не установлены)
dotnet tool install --global dotnet-ef

# Создание начальной миграции
dotnet ef migrations add InitialCreate

# Применение миграций
dotnet ef database update